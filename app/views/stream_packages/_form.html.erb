<%= form_with(model: stream_package, local: true) do |form| %>
  <% if stream_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stream_package.errors.count, "error") %> prohibited this stream_package from being saved:</h2>

      <ul>
      <% stream_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
